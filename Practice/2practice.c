#include <stdio.h>

int main()
{
    int r = 3;
    float pi = 3.14;

    printf("The area of circle is %f\n", pi * r * r);
    int h = 3;

    printf("The volume of cylinder is %f\n", pi * r * r * h);
    return 0;
}